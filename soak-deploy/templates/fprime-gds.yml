command-line-options:
  # store_true flags: use null so the parser emits just `--flag` without a value.
  ip-client: null
  no-app: null
  ip-address: 127.0.0.1
  gui: none
  deployment: __INSTALL_DIR__
  logs: __INSTALL_DIR__/gds-logs
